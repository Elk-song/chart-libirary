// 图表类型枚举
export enum EChartType {
  MSLine = 0,
  MSBar = 5,
}
// 图表数据类型
export enum EChartDataType {
  static = 0,
  dynamic = 5,
}
export enum EThemeType {
  walden = 0,
  vintage = 5,
  westeros = 10,
  shine = 15,
  macarons = 20,
}
export enum ERequest {
  get = 0,
  post = 5,
  put = 10,
  delete = 15,
}
